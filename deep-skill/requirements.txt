# deep-skill v0.0.1 — Go-focused
# Python 3.10+
# Needs the Query()/QueryCursor API (matches()/captures() on the cursor),
# introduced in the 0.24+ line. Verified on tree-sitter 0.25.2 / tree-sitter-go 0.25.0.
tree-sitter>=0.25
tree-sitter-go>=0.23
